<!-- AGENTTEAMS:BEGIN content v=1 -->
# SETUP-REQUIRED.md

The following **21 placeholder(s)** could not be automatically resolved
for project **ResearchTeam** and require manual attention.

---

## 1. `{CONVERSION_PIPELINE}`

**Found in:** `multiple`
**Context:** The placeholder {CONVERSION_PIPELINE} could not be auto-resolved.

**Action required:** Search for `{MANUAL:CONVERSION_PIPELINE}` across all generated
agent files and replace with the correct value.

---

## 2. `{PIP_PACKAGE_NAME}`

**Found in:** `multiple`
**Context:** The placeholder {PIP_PACKAGE_NAME} could not be auto-resolved.

**Action required:** Search for `{MANUAL:PIP_PACKAGE_NAME}` across all generated
agent files and replace with the correct value.

---

## 3. `{DOC_SITE_CONFIG_FILE}`

**Found in:** `multiple`
**Context:** The placeholder {DOC_SITE_CONFIG_FILE} could not be auto-resolved.

**Action required:** Search for `{MANUAL:DOC_SITE_CONFIG_FILE}` across all generated
agent files and replace with the correct value.

---

## 4. `{TOOL_DOCS_URL}`

**Found in:** `../skills/tool-pandoc.md`
**Context:** The skill document for 'Pandoc' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 5. `{TOOL_API_SURFACE}`

**Found in:** `../skills/tool-pandoc.md`
**Context:** The skill document for 'Pandoc' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 6. `{TOOL_COMMON_PATTERNS}`

**Found in:** `../skills/tool-pandoc.md`
**Context:** The skill document for 'Pandoc' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 7. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-bibtex-reference.md`
**Context:** The reference file for 'BibTeX' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 8. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-bibtex-reference.md`
**Context:** The reference file for 'BibTeX' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 9. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-bibtex-reference.md`
**Context:** The reference file for 'BibTeX' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 10. `{COMPONENT_SPEC}`

**Found in:** `topic-scoping-expert.agent.md`
**Context:** Component 'Topic Scoping and Research Plan' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 11. `{COMPONENT_SECTIONS}`

**Found in:** `topic-scoping-expert.agent.md`
**Context:** Component 'Topic Scoping and Research Plan' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 12. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `topic-scoping-expert.agent.md`
**Context:** Component 'Topic Scoping and Research Plan' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 13. `{COMPONENT_SPEC}`

**Found in:** `literature-review-expert.agent.md`
**Context:** Component 'Literature Review' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 14. `{COMPONENT_SECTIONS}`

**Found in:** `literature-review-expert.agent.md`
**Context:** Component 'Literature Review' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 15. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `literature-review-expert.agent.md`
**Context:** Component 'Literature Review' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 16. `{COMPONENT_SPEC}`

**Found in:** `main-analysis-expert.agent.md`
**Context:** Component 'Main Analysis' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 17. `{COMPONENT_SECTIONS}`

**Found in:** `main-analysis-expert.agent.md`
**Context:** Component 'Main Analysis' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 18. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `main-analysis-expert.agent.md`
**Context:** Component 'Main Analysis' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 19. `{COMPONENT_SPEC}`

**Found in:** `conclusion-expert.agent.md`
**Context:** Component 'Conclusion and Executive Summary' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 20. `{COMPONENT_SECTIONS}`

**Found in:** `conclusion-expert.agent.md`
**Context:** Component 'Conclusion and Executive Summary' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 21. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `conclusion-expert.agent.md`
**Context:** Component 'Conclusion and Executive Summary' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---


Once all items above are resolved, invoke `@conflict-auditor` to verify consistency.

## Recommended `.gitignore` entries

Your generated team writes two machine-local caches that should **not** be committed. Add these
to this repository's `.gitignore`:

```gitignore
# agentteams retrieval validation-cache sidecars — machine-local, rebuilt on demand
**/references/memory-index.vcache
**/references/code-index/
```

Already committed one by mistake? `.gitignore` does not untrack an existing file — use
`git rm --cached <path>` first, then confirm with `git ls-files | grep vcache`.
<!-- AGENTTEAMS:END content -->
